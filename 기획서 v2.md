# 삼성화재 주식 현황판 → 개인 투자관리도구 기획서 (v2)

기존 삼성화재 단일 종목용 엑셀 스타일 현황판(`samsung-fire-stock.html`)을
여러 종목을 다루는 개인용 투자관리도구로 확장하기 위한 기획서.

> **v1 대비 변경점**: 데이터 저장 방식·시세 출처·기술 스택을 확정하고,
> 데이터 모델 / 계산 규칙 / 엣지 케이스 / 완료 기준 / 2주 일정을 추가했습니다.
> v1은 "무엇을 만들지"까지만 있었고, 신입 개발자가 착수하려면 필요한
> "어떻게 만들지"의 결정들이 비어 있었습니다.

---

## 0. 선행 확인 사항 (착수 전 반드시 해결)

- **`samsung-fire-stock.html`이 현재 저장소에 없습니다.** (저장소에는 이 기획서와
  `README.md`만 존재)
  - 기획서는 "현재 삼성화재 화면과 유사"를 기준으로 삼고 있으나, 그 기준 파일이
    없어 신입이 참고할 대상이 없는 상태입니다.
  - **조치**: 기존 HTML 파일을 저장소에 커밋하거나, 없다면 "기존 화면 참고" 요구사항을
    삭제하고 이 문서의 화면 정의만으로 진행.

---

## 1. 문제

- **여러 종목 관리 불가**: 지금은 삼성화재 하나만 볼 수 있어서, 여러 종목을 동시에
  관리·비교하기 어려움
- **엑셀/증권앱 파편화**: 매수 기록은 엑셀에, 시세는 증권앱에 따로 있어서 한 곳에서
  의사결정하기 어려움

## 2. 쓰는 사람

- **나 혼자만 (개인 전용)**: 본인만 업무 중에 몰래 켜놓고 쓰는 개인용 투자 관리 도구
- 다중 사용자·로그인·권한 개념 **없음**. 계정 기능을 만들지 않습니다.

---

## 3. 1차 버전 범위 (Scope)

기능을 늘리는 것보다 **경계를 긋는 것**이 이 프로젝트의 성패를 가릅니다.

### 포함 (In scope)

| 항목 | 결정 |
|---|---|
| 종목 등록/삭제 | O |
| 매수·매도 기록 입력/조회 | O |
| 포트폴리오 합산(평가금액·손익·수익률) | O |
| 분기별 주가 추이 차트 | O (수동 입력 데이터 기반) |
| 데이터 저장 | 브라우저 `localStorage` |
| 시세 데이터 | **수동 입력** |

### 제외 (Out of scope — Phase 2 이월)

- 목표수익률 달성 트래킹 *(v1에서 이미 이월 결정)*
- **실시간 시세 API 자동 연동**
- 서버 / 데이터베이스 / 로그인
- 매매 수수료·거래세·배당 반영
- 해외주식, 환율, 다중 통화
- 모바일 반응형 (데스크톱 브라우저 1개 해상도 기준으로만 작업)

### 왜 시세를 수동 입력으로 정했나

실시간 시세 API 연동은 이 프로젝트에서 **난이도를 가장 크게 좌우하는 단일 결정**입니다.

- 국내 시세 API는 대부분 인증키 발급, 요청 수 제한, 브라우저 직접 호출 시 CORS 차단
  문제가 따라옵니다. 이를 우회하려면 프록시 서버가 필요해지고, 그 순간 "순수
  프론트엔드 도구"라는 전제가 무너집니다.
- 신입 개발자가 2주 안에 이 문제까지 해결하기는 어렵습니다.
- 1차 버전은 **현재가와 분기별 종가를 사용자가 직접 입력**하는 방식으로 가고,
  API 연동은 Phase 2로 넘깁니다. 이렇게 하면 "데이터를 어디서 가져오는가"와
  "데이터를 어떻게 계산·표시하는가"가 분리되어, 나중에 API를 붙일 때
  입력 부분만 교체하면 됩니다.

---

## 4. 기술 스택 (확정)

| 항목 | 결정 | 이유 |
|---|---|---|
| 언어/구조 | 순수 HTML + CSS + JavaScript (프레임워크 없음) | 기존 단일 HTML 스타일과 일관, 러닝커브 제거 |
| 화면 전환 | 라우팅 없이 **탭/뷰 전환** (한 페이지 내 `display` 토글) | SPA 라우터 학습 비용 제거 |
| 차트 | Chart.js (CDN 로드) 1개만 사용 | 차트 자체 구현은 시간 초과 위험이 큼 |
| 저장소 | `localStorage` (JSON 직렬화) | 서버 없이 데이터 영속화 |
| 빌드 도구 | 없음 (파일 열면 바로 동작) | 번들러 설정 시간 절약 |

> **주의**: 프레임워크(React/Vue) 도입은 명시적으로 **금지**합니다. 신입이 스스로
> "이 정도면 React가 낫지 않을까" 고민하는 데 며칠을 쓰는 것을 막기 위한 결정입니다.

### localStorage의 한계 (알고 쓰기)

- 브라우저/기기를 바꾸면 데이터가 따라오지 않습니다.
- 브라우저 데이터 삭제 시 기록이 사라집니다.
- → **완충 장치**: "JSON 내보내기 / 가져오기" 버튼을 넣어 백업 가능하게 합니다.
  (구현 난이도 낮음, 데이터 유실 리스크는 크게 낮춤 — 1차 범위에 포함)

---

## 5. 핵심 기능 (3개)

1. **여러 종목 통합 포트폴리오**: 종목을 추가/삭제하며, 전체 평가금액·수익률을
   합산해서 보여줌
2. **매매 기록/히스토리 관리**: 매수·매도 이력을 남겨 시점별 수익을 복기할 수 있음
3. **분기별 주가 추이 차트**: 종목별로 과거~현재 시세 추이를 차트로 시각화

---

## 6. 데이터 모델

`localStorage` 키 하나(`portfolio-data`)에 아래 구조를 JSON으로 저장합니다.

```js
{
  "version": 1,               // 향후 구조 변경 시 마이그레이션용
  "stocks": [
    {
      "id": "s_1712345678901",   // 고유 ID (생성 시각 기반)
      "ticker": "000810",        // 종목코드
      "name": "삼성화재",         // 종목명
      "currentPrice": 385000,    // 현재가 (수동 입력)
      "priceUpdatedAt": "2026-08-25",
      "quarterlyPrices": [       // 분기별 종가 (차트용, 수동 입력)
        { "quarter": "2025Q1", "close": 342000 },
        { "quarter": "2025Q2", "close": 358000 }
      ]
    }
  ],
  "trades": [
    {
      "id": "t_1712345678902",
      "stockId": "s_1712345678901",  // stocks[].id 참조
      "type": "BUY",                 // "BUY" | "SELL"
      "date": "2025-03-14",
      "price": 342000,               // 체결 단가
      "quantity": 10,
      "memo": "분할매수 1차"          // 선택 입력
    }
  ]
}
```

### 설계 포인트

- **보유 수량과 평단가는 저장하지 않습니다.** `trades`에서 매번 계산합니다.
  (저장하면 기록 수정 시 값이 어긋나는 버그가 반드시 생깁니다)
- 종목과 매매기록은 `stockId`로 연결된 별도 배열입니다.

---

## 7. 계산 규칙 (가장 중요 — 여기서 막히면 일정이 밀립니다)

### 7-1. 평균단가 — 이동평균법

분할매수를 하면 평단가가 바뀌므로 규칙을 명확히 정합니다.

```
매수 시:
  새 평단가 = (기존 평단가 × 기존 수량 + 매수단가 × 매수수량)
              ÷ (기존 수량 + 매수수량)
  새 수량   = 기존 수량 + 매수수량

매도 시:
  평단가 = 변하지 않음 (그대로 유지)
  새 수량 = 기존 수량 - 매도수량
```

> 매도 시 평단가를 건드리지 않는 것이 핵심입니다. 매도는 "원가를 덜어내는" 행위이지
> 원가 단가를 바꾸는 행위가 아닙니다.

**계산 방식**: `trades`를 **날짜 오름차순으로 정렬**한 뒤 처음부터 순차 적용합니다.
(입력 순서가 아니라 거래일 기준. 과거 거래를 나중에 입력할 수 있기 때문)

### 7-2. 손익 계산

```
보유수량   = Σ매수수량 - Σ매도수량
매수원금   = 평단가 × 보유수량
평가금액   = 현재가 × 보유수량
평가손익   = 평가금액 - 매수원금
수익률(%)  = 평가손익 ÷ 매수원금 × 100

실현손익(건별) = (매도단가 - 매도 시점의 평단가) × 매도수량
종목 실현손익  = Σ 건별 실현손익
```

### 7-3. 포트폴리오 합계

```
총 매수원금 = Σ(종목별 매수원금)
총 평가금액 = Σ(종목별 평가금액)
총 평가손익 = 총 평가금액 - 총 매수원금
총 수익률   = 총 평가손익 ÷ 총 매수원금 × 100
```

### 7-4. 표시 규칙

- 금액: 천 단위 콤마, 원 단위 (`385,000원`)
- 수익률: 소수점 2자리 (`+12.34%`), 부호 항상 표시
- 색상: 이익 빨강, 손실 파랑 (국내 증권앱 관례)
- **수수료·세금은 1차 버전에서 계산하지 않습니다.** (Phase 2)

---

## 8. 검증 규칙 및 엣지 케이스

구현 전에 이 표를 먼저 읽어야 합니다. 화면은 만들어도 여기서 막히는 경우가 많습니다.

| 상황 | 처리 |
|---|---|
| 매도 수량 > 현재 보유 수량 | 저장 거부 + "보유 수량(N주)을 초과합니다" 안내 |
| 보유 수량이 남은 종목 삭제 시도 | 확인 대화상자로 재확인 후 삭제 (기록도 함께 삭제) |
| 매매 기록이 0건인 종목 | 보유 0주 / 평가금액 0원 / **수익률은 `-` 표시** |
| 보유 수량 0 (전량 매도) | 목록에는 유지, 평가손익 0, 실현손익만 표시 |
| 매수원금이 0인데 수익률 계산 | **0으로 나누기 방지** → `-` 표시 |
| 현재가 미입력 종목 | 평가금액 계산에서 제외 + 목록에 "시세 미입력" 배지 |
| 종목코드 중복 등록 | 저장 거부 + "이미 등록된 종목입니다" |
| 가격/수량에 음수·0·문자 입력 | 저장 거부 + 필드별 오류 메시지 |
| 미래 날짜 매매 기록 | 저장 거부 |
| localStorage 파싱 실패(손상) | 빈 상태로 초기화 + 경고 표시 (앱이 죽지 않게) |
| 분기별 종가가 1개 이하 | 차트 대신 "데이터가 부족합니다" 안내 |

---

## 9. 화면 구성 및 완료 기준

각 화면은 아래 완료 기준(Acceptance Criteria)을 **모두** 만족해야 "완료"입니다.

### 9-1. 종목 추가/관리 화면

- 종목코드, 종목명, 현재가를 입력해 등록할 수 있다
- 등록된 종목 목록이 보이고, 각 항목을 삭제할 수 있다
- 현재가를 수정할 수 있다
- 8장의 검증 규칙(중복 등록, 음수 입력, 보유 종목 삭제 확인)이 동작한다
- 새로고침해도 등록한 종목이 남아 있다

### 9-2. 포트폴리오 전체 요약 화면 (메인)

- 상단에 총 매수원금 / 총 평가금액 / 총 평가손익 / 총 수익률이 표시된다
- 보유 종목이 표로 나열되고, 종목별 보유수량·평단가·현재가·평가손익·수익률이 보인다
- 손익에 따라 색상(빨강/파랑)이 적용된다
- 종목이 0개일 때 "종목을 먼저 등록하세요" 안내가 나온다
- 종목 행을 클릭하면 상세 화면으로 이동한다

### 9-3. 종목별 상세 화면

- 해당 종목의 보유수량·평단가·현재가·평가손익·실현손익이 표시된다
- 분기별 주가 추이 차트가 그려진다
- 해당 종목의 매매 이력이 최신순으로 나열된다
- 분기별 종가를 추가/수정할 수 있다
- 요약 화면으로 돌아가는 버튼이 있다

### 9-4. 매매 기록 입력/조회 화면

- 종목 선택 / 매수·매도 구분 / 날짜 / 단가 / 수량 / 메모를 입력해 저장할 수 있다
- 전체 매매 이력을 날짜순으로 조회할 수 있다
- 종목별·매매구분별 필터가 동작한다
- 기록을 삭제할 수 있고, 삭제 시 평단가·수익률이 즉시 재계산된다
- 8장의 검증 규칙(초과 매도, 미래 날짜, 음수)이 동작한다

### 9-5. 공통

- 4개 화면 간 이동이 되고, 현재 화면이 표시된다
- JSON 내보내기 / 가져오기가 동작한다

---

## 10. 만드는 순서 및 2주 일정

10 근무일 기준. 순서는 v1과 동일하며, 앞에 **0일차(설계 확정)** 를 추가했습니다.

| 일차 | 작업 | 산출물 |
|---|---|---|
| 1일차 | 데이터 모델·계산 규칙 숙지, HTML 뼈대 + 화면 전환 + localStorage 읽기/쓰기 유틸 | 빈 화면 4개 전환 동작 |
| 2일차 | 종목 추가/삭제/수정 + 검증 | 9-1 완료 |
| 3일차 | 계산 로직(평단가·손익) 함수 작성 및 **수동 테스트** | 계산 함수 검증 완료 |
| 4~5일차 | 포트폴리오 요약 화면 | 9-2 완료 |
| 6일차 | 종목 상세 화면 (차트 제외) | 9-3 일부 |
| 7일차 | Chart.js 연동, 분기별 종가 입력/차트 | 9-3 완료 |
| 8~9일차 | 매매 기록 입력/조회/필터/삭제 | 9-4 완료 |
| 10일차 | JSON 내보내기/가져오기, 엣지 케이스 점검, 마무리 | 9-5 완료 |

**3일차를 별도로 뺀 이유**: 평단가·수익률 계산은 화면보다 먼저, 화면과 분리해서
검증해야 합니다. 화면에 섞어서 만들면 숫자가 틀렸을 때 원인이 계산인지 표시인지
구분이 안 되어 디버깅에 하루가 사라집니다.

---

## 11. 신입사원 2주 완성 가능성 평가

**결론: 아래 조건이 지켜지면 가능합니다. 지켜지지 않으면 어렵습니다.**

### 가능하게 만드는 조건

- 시세는 수동 입력, 실시간 API 연동 없음 (4장)
- `localStorage`만 사용, 서버/DB 없음
- 프레임워크 없이 순수 HTML/JS 유지
- 차트는 Chart.js 하나만 사용
- **이 문서의 결정 사항들을 착수 전에 확정** — 신입이 스스로 정하게 두면
  설계 단계에서만 며칠이 사라집니다

### 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 평단가·수익률 계산 로직에서 막힘 | 1~2일 지연 | 7장 공식을 그대로 제공, 3일차에 집중 배치 |
| 엣지 케이스를 뒤늦게 발견해 구조 수정 | 1~2일 지연 | 8장 표를 착수 전에 읽히기 |
| 차트 라이브러리 삽질 | 1일 지연 | Chart.js 기본 line chart만 사용, 커스터마이징 금지 |
| 요구사항이 도중에 늘어남 | 일정 붕괴 | 3장 "제외" 목록을 변경 금지선으로 고정 |

### 권고

- **버퍼 1일**을 확보하거나, 10일차 작업(JSON 내보내기/가져오기)을 선택 항목으로
  둡니다.
- **멘토링 없이 완전히 혼자** 맡기면 빠듯합니다. 최소한 3일차(계산 로직)와
  5일차(요약 화면) 종료 시점에 코드 리뷰를 1회씩 넣는 것을 권장합니다.
- 반대로 **시세 API 자동 연동이나 서버 저장을 1차 범위에 넣으면 2주로는 부족합니다.**

---

## 12. Phase 2 이월 항목

- 목표수익률 달성 트래킹
- 실시간 시세 API 자동 연동
- 매매 수수료·거래세·배당 반영
- 종목별 비중(파이 차트), 자산 배분 분석
- 서버/DB 저장 및 기기 간 동기화
- 모바일 반응형
